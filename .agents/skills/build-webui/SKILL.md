---
name: build-webui
description: Build and validate web UI dashboard
---

Build web UI: cd into web_ui/ and run `npm run typecheck && npm run lint && npm run build`. Fix any TypeScript or lint errors before retrying.
