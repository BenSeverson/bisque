# Bisque — Kiln Controller Firmware

See **[CLAUDE.md](./CLAUDE.md)** for all project guidance: build & flash, testing, the
display simulator, code style, project structure, and the LVGL UI/theme conventions.

This file previously carried its own copy of that guidance and drifted badly out of sync
with the code (it still described a dark theme and inline-only styling long after the UI
moved to a light theme driven by `components/display/ui_theme.c`). Keep the guidance in
CLAUDE.md only — do not re-fork it here.
